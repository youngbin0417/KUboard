#include <stdio.h>
#define MAX 30;

bool correct(const char* que) {
	char arr[MAX];
	gets_s(arr, MAX);
	if (strcmp(const char* que, const char* arr) == 0) {
		return 1; // 두문자가 일치할 때
	}
	else
		return 0;
}

int main() {
	printf("hello world");

	return 0;
}
